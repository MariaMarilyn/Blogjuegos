from django.urls import path
from . import views

urlpatterns = [
    # Define las URLs para las categorías si es necesario
    # path('', views.CategoryListView.as_view(), name='category-list'),
    # path('<int:pk>/', views.CategoryDetailView.as_view(), name='category-detail'),
]
